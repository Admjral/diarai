# Скрипты для настройки окружения

## check-env.js

Проверяет наличие всех обязательных переменных окружения.

**Использование:**
```bash
npm run check-env
```

**Что проверяет:**
- Обязательные переменные: `DATABASE_URL`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NODE_ENV`
- Опциональные переменные: `OPENAI_API_KEY`, `SENTRY_DSN`, `LOG_LEVEL`, `FRONTEND_URL`, и др.

**Результат:**
- ✅ Показывает настроенные переменные (значения секретов скрыты)
- ❌ Показывает отсутствующие переменные
- 💡 Дает инструкции по настройке

## setup-local-env.js

Интерактивный скрипт для настройки локального `.env` файла.

**Использование:**
```bash
npm run setup-local-env
```

**Что делает:**
1. Проверяет существующий `.env` файл
2. Интерактивно запрашивает значения для обязательных переменных
3. Сохраняет значения в `.env` файл
4. Скрывает ввод для секретных значений (пароли, ключи)

**Особенности:**
- Сохраняет существующие значения, если они уже настроены
- Показывает подсказки, откуда взять каждое значение
- Скрывает ввод для секретных переменных

## setup-local-env.js

Интерактивный скрипт для настройки локального `.env` файла.

**Использование:**
```bash
npm run setup-local-env
```

**Что делает:**
1. Проверяет существующий `.env` файл
2. Интерактивно запрашивает значения для обязательных переменных
3. Сохраняет значения в `.env` файл
4. Скрывает ввод для секретных значений (пароли, ключи)

**Особенности:**
- Сохраняет существующие значения, если они уже настроены
- Показывает подсказки, откуда взять каждое значение
- Скрывает ввод для секретных переменных

## setup-vercel-env.sh

Интерактивный скрипт для настройки переменных окружения на Vercel.

**Использование:**
```bash
npm run setup-vercel-env
```

**Что делает:**
1. Проверяет наличие Vercel CLI
2. Проверяет авторизацию в Vercel
3. Интерактивно запрашивает значения для обязательных переменных
4. Добавляет переменные для Production окружения
5. Опционально добавляет для Preview окружения

**Требования:**
- Установлен Vercel CLI: `npm i -g vercel`
- Авторизован в Vercel: `vercel login`

## verify-setup.js

Комплексная проверка готовности системы к работе.

**Использование:**
```bash
npm run verify
```

**Что проверяет:**
1. Наличие всех обязательных переменных окружения
2. Подключение к базе данных (Prisma)
3. Создание Supabase клиента
4. Наличие и авторизация Vercel CLI

**Результат:**
- ✅ Показывает статус каждой проверки
- 📊 Выводит итоговую сводку
- 💡 Дает рекомендации по следующим шагам

## Быстрый старт

### Локальная настройка

```bash
# 1. Интерактивная настройка локального .env файла
npm run setup-local-env

# 2. Проверить переменные
npm run check-env

# 3. Проверить готовность системы (опционально)
npm run verify
```

### Настройка на Vercel (продакшен)

```bash
# 1. Настроить переменные на Vercel (интерактивно)
npm run setup-vercel-env

# 2. Перезапустить деплой
vercel --prod

# 3. Проверить работу
curl https://server-wgba.vercel.app/health
```

### Альтернативный способ (ручной)

```bash
# 1. Создать .env файл из template (если еще не создан)
npm run setup-env

# 2. Вручную заполнить server/.env

# 3. Проверить переменные
npm run check-env
```

