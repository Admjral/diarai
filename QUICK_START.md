# 🚀 Быстрый старт DIAR AI

Краткая инструкция по настройке и запуску проекта.

## Предварительные требования

- Node.js 18+ и npm
- Аккаунт Supabase (для базы данных и аутентификации)
- (Опционально) OpenAI API ключ (для AI функций)

## Автоматическая настройка (рекомендуется)

Запустите главный скрипт настройки:

```bash
chmod +x setup-all.sh
./setup-all.sh
```

Скрипт автоматически:
- ✅ Проверит Node.js и npm
- ✅ Установит зависимости для фронтенда и бэкенда
- ✅ Создаст .env файлы из шаблонов (если отсутствуют)
- ✅ Сгенерирует Prisma Client
- ✅ Проверит миграции и переменные окружения

## Ручная настройка

### 1. Установка зависимостей

```bash
# Установка зависимостей фронтенда
npm install

# Установка зависимостей бэкенда
cd server
npm install
cd ..
```

### 2. Настройка переменных окружения

#### Backend (server/.env)

Создайте файл `server/.env` на основе `server/env.template`:

```bash
cd server
cp env.template .env
```

Заполните обязательные переменные:
- `DATABASE_URL` - из Supabase Dashboard → Database → Connection string → URI
- `SUPABASE_URL` - из Supabase Dashboard → Settings → API → Project URL
- `SUPABASE_SERVICE_ROLE_KEY` - из Supabase Dashboard → Settings → API → service_role key

#### Frontend (.env)

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
VITE_API_URL=http://localhost:3001
```

Где получить:
- Откройте: https://supabase.com/dashboard/project/YOUR_PROJECT_REF/settings/api
- Скопируйте `Project URL` → `VITE_SUPABASE_URL`
- Скопируйте `anon` `public` ключ → `VITE_SUPABASE_ANON_KEY`

### 3. Генерация Prisma Client

```bash
cd server
npm run prisma:generate
```

Или используйте скрипт:
```bash
cd server
chmod +x setup-prisma.sh
./setup-prisma.sh
```

### 4. Применение миграций базы данных

```bash
cd server
npm run prisma:migrate
```

Это создаст все необходимые таблицы в базе данных.

### 5. (Опционально) Настройка Row Level Security (RLS)

1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте содержимое `server/ENABLE_RLS.sql` или `server/ENABLE_RLS_SIMPLE.sql`
3. Выполните SQL

### 6. (Опционально) Настройка Storage Bucket

Для хранения изображений кампаний:
1. Откройте Supabase Dashboard → Storage
2. Создайте bucket `campaign-images`
3. Настройте публичный доступ и RLS политики

## Проверка настройки

### Проверка переменных окружения

```bash
cd server
node check-env.js
```

### Проверка миграций

```bash
cd server
node check-migrations.js
```

## Запуск приложения

### Development режим

Откройте два терминала:

**Терминал 1 (Backend):**
```bash
cd server
npm run dev
```

Сервер будет доступен на `http://localhost:3001`

**Терминал 2 (Frontend):**
```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173` (или другом порту, указанном Vite)

## Полезные команды

### Backend

```bash
cd server

# Генерация Prisma Client
npm run prisma:generate

# Применение миграций
npm run prisma:migrate

# Открыть Prisma Studio (GUI для БД)
npm run prisma:studio

# Заполнить тестовыми данными
npm run prisma:seed

# Проверка переменных окружения
node check-env.js

# Проверка миграций
node check-migrations.js

# Создание .env файлов
node setup-env.js
```

### Frontend

```bash
# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build
```

## Решение проблем

### Ошибка: "Модель Wallet не найдена в Prisma Client"

**Решение:**
```bash
cd server
npm run prisma:generate
# Затем перезапустите сервер
```

### Ошибка: "DATABASE_URL не установлен"

**Решение:**
1. Создайте `server/.env` из шаблона
2. Заполните `DATABASE_URL` из Supabase Dashboard
3. Проверьте: `cd server && node check-env.js`

### Ошибка подключения к базе данных

**Решение:**
1. Проверьте правильность `DATABASE_URL` в `server/.env`
2. Убедитесь, что пароль в URL верный
3. Проверьте Network Restrictions в Supabase (должны быть отключены для разработки)

### CORS ошибки

**Решение:**
1. Убедитесь, что `VITE_API_URL` в `.env` указывает на правильный URL бэкенда
2. Для продакшена установите `FRONTEND_URL` в `server/.env`

## Следующие шаги

После успешного запуска:

1. ✅ Создайте аккаунт через форму регистрации
2. ✅ Протестируйте основные функции:
   - Создание лидов
   - Создание сделок
   - Создание задач
   - Создание кампаний
   - Работа с кошельком
3. ✅ (Опционально) Настройте OpenAI API для AI функций
4. ✅ (Опционально) Настройте интеграцию с Kaspi.kz для платежей

## Дополнительная документация

- [README.md](README.md) - Общая документация проекта
- [ЧЕКЛИСТ_ГОТОВНОСТИ.md](ЧЕКЛИСТ_ГОТОВНОСТИ.md) - Полный чеклист настройки
- [PROJECT_STATUS.md](PROJECT_STATUS.md) - Статус проекта
- [server/README.md](server/README.md) - Документация по серверу
- [SUPABASE_SETUP.md](SUPABASE_SETUP.md) - Настройка Supabase

## Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12) для ошибок фронтенда
2. Проверьте логи сервера в терминале
3. Проверьте переменные окружения: `cd server && node check-env.js`
4. Проверьте миграции: `cd server && node check-migrations.js`

---

**Готово!** Теперь ваш проект должен работать. 🎉


